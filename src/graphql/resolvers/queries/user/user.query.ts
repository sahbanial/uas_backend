import { ClientModel } from '../../../../models';
import { getQueryInfos } from '../../../util/functions';
import { collectionsNames } from '../../../util/collectionFileds';

export default (_, {}, { user: { id, profile: { id: uid } } }, infos) =>
	new Promise((resolve, reject) => {
		
		
	});
